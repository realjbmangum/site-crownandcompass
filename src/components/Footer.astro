---
---

<footer class="site-footer">
  <div class="footer-inner">
    <div class="footer-brand">
      <img src="/cc.png" alt="" class="footer-logo" aria-hidden="true" />
      <p class="footer-name">Crown &amp; Compass</p>
      <p class="footer-tagline">The Walk. Long. Slow. Together.</p>
    </div>

    <nav class="footer-nav" aria-label="Footer navigation">
      <a href="/compass">The Compass</a>
      <a href="/books">Books</a>
      <a href="/about">About</a>
      <a href="/join">Join</a>
    </nav>

    <p class="footer-copy">
      &copy; {new Date().getFullYear()} Crown and Compass. Free because the Gospel is free.
    </p>
  </div>
</footer>

<style>
  .site-footer {
    background-color: var(--bg-base);
    border-top: 1px solid var(--border);
    box-shadow: 0 -1px 0 0 rgba(201, 133, 58, 0.15);
    padding: 3rem 1.5rem;
    margin-top: auto;
  }

  .footer-inner {
    max-width: 68rem;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
    text-align: center;
  }

  .footer-brand {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.375rem;
  }

  .footer-logo {
    height: 64px;
    width: auto;
    opacity: 0.85;
    margin-bottom: 0.25rem;
  }

  .footer-name {
    font-family: 'EB Garamond', Georgia, serif;
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--text-primary);
    margin: 0;
  }

  .footer-tagline {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-family: 'EB Garamond', Georgia, serif;
    font-size: 0.9375rem;
    color: var(--text-muted);
    font-style: italic;
    margin: 0;
  }

  .footer-tagline::before,
  .footer-tagline::after {
    content: '';
    width: 1.5rem;
    height: 1px;
    background-color: var(--accent);
    opacity: 0.3;
    flex-shrink: 0;
  }

  .footer-nav {
    display: flex;
    gap: 1.75rem;
  }

  .footer-nav a {
    font-family: 'DM Sans', system-ui, sans-serif;
    font-size: 0.875rem;
    color: var(--text-muted);
    text-decoration: none;
    transition: color 0.15s ease;
  }

  .footer-nav a:hover {
    color: var(--text-primary);
  }

  .footer-copy {
    font-family: 'DM Sans', system-ui, sans-serif;
    font-size: 0.8125rem;
    color: var(--text-muted);
    opacity: 0.6;
    margin: 0;
  }
</style>
