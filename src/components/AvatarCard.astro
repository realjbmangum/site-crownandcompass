---
interface Props {
  headline: string;
  description: string;
  number: string;
}

const { headline, description, number } = Astro.props;
---

<div class="avatar-card">
  <div class="card-number" aria-hidden="true">{number}</div>
  <h3 class="avatar-headline">{headline}</h3>
  <p class="avatar-description">{description}</p>
</div>

<style>
  .avatar-card {
    background-color: var(--bg-card);
    border: 1px solid var(--cc-border, #2e2c26);
    padding: 2rem;
    display: flex;
    flex-direction: column;
    gap: 0.875rem;
  }

  .card-number {
    font-family: 'EB Garamond', Georgia, serif;
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--accent);
    opacity: 0.45;
    line-height: 1;
    margin-bottom: 0.25rem;
  }

  .avatar-headline {
    font-family: 'EB Garamond', Georgia, serif;
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--text-primary);
    line-height: 1.3;
    margin: 0;
  }

  .avatar-description {
    font-family: 'DM Sans', system-ui, sans-serif;
    font-size: 0.9375rem;
    color: var(--text-muted);
    line-height: 1.7;
    margin: 0;
    font-weight: 300;
  }
</style>
